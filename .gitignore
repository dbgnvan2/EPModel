.venv/
__pycache__/
*.pyc
.env
.DS_Store

# engine audit output — written by the engine at runtime (see CLAUDE.md,
# engine purity); the stray copies are a known defect, not artefacts to track
sim_audit.csv

# agent worktrees
.claude/worktrees/
